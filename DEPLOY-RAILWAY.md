# دليل نشر منصة «مير» على Railway — خطوة بخطوة

> هذا الدليل لمن لا يملك خبرة برمجية. كل خطوة موضّحة بالتفصيل.
> الوقت المتوقع: 30–45 دقيقة
> التكلفة: ~5 دولارات/شهر (أو مجاني للشهر الأول مع رصيد الترحيب)

---

## 📋 ما ستحتاجه قبل البدء

1. **بطاقة ائتمان** (لتفعيل حساب Railway — لن يُخصم شيء في الشهر الأول)
2. **بريد إلكتروني** لإنشاء الحسابات
3. **ملف المشروع** `mir-platform.tar.gz` (سأجهّزه لك)

---

## الخطوة 1: حمّل كود المشروع

في مجلد المشروع ستجد ملفًا اسمه:
```
mir-platform.tar.gz
```

حمّله إلى جهازك الكمبيوتر (سأوضّح لك كيف تحصل عليه).

---

## الخطوة 2: أنشئ حساب GitHub وارفع الكود

GitHub هو موقع يخزّن الكود. Railway سيأخذ الكود منه تلقائيًا.

### 2-أ. أنشئ حساب GitHub
1. اذهب إلى https://github.com/signup
2. أدخل بريدك، اختر اسم مستخدم وكلمة مرور
3. أكّد الحساب عبر البريد

### 2-ب. أنشئ مستودعًا جديدًا
1. بعد الدخول، اضغط زر **`+`** في أعلى يمين الصفحة → **New repository**
2. اسم المستودع: `mir-platform`
3. اختر **Private** (خاص — لا يراه أحد غيرك)
4. ✅ علّم **Add a README file**
5. اضغط **Create repository**

### 2-ج. ارفع ملفات المشروع

**الطريقة الأسهل (بدون برامج):**
1. فكّ ضغط `mir-platform.tar.gz` على جهازك (بالضغط عليه مرتين)
2. في صفحة مستودع GitHub، اضغط **Add file** → **Upload files**
3. اسحب كل الملفات والمجلدات من المجلد المفكوك إلى نافذة GitHub
   - **ملاحظة**: GitHub لا يقبل رفع مجلدات فارغة. إذا واجهت مشكلة، استخدم الطريقة التالية.
4. في أسفل الصفحة، اكتب رسالة: `رفع منصة مير`
5. اضغط **Commit changes**

**الطريقة الأفضل (عبر GitHub Desktop):**
1. حمّل GitHub Desktop من https://desktop.github.com (مجاني، يعمل على ويندوز وماك)
2. ثبّته وسجّل دخولك بحساب GitHub
3. اضغط **File → Clone repository** → اختر `mir-platform` → **Clone**
4. ستفتح نافذة على جهازك فيها ملفات المستودع
5. انسخ كل محتويات `mir-platform` (المفكوكة) إلى هذه النافذة
6. في GitHub Desktop، ستظهر الملفات الجديدة. اكتب رسالة: `رفع منصة مير`
7. اضغط **Commit to main** ثم **Push origin** (في الأعلى)

✅ الآن الكود على GitHub.

---

## الخطوة 3: أنشئ حساب Railway

1. اذهب إلى https://railway.app
2. اضغط **Login** → **Login with GitHub** (استخدم حساب GitHub الذي أنشأته)
3. امنح Railway الصلاحية للوصول إلى حسابك
4. أكّد بريدك الإلكتروني

✅ Railway يعطيك رصيدًا ترحيبيًا مجانيًا (~5 دولارات).

---

## الخطوة 4: أنشئ قاعدة بيانات PostgreSQL

1. من لوحة Railway الرئيسية، اضغط **New Project** (أعلى يمين)
2. اختر **Provision PostgreSQL**
3. Railway سينشئ قاعدة بيانات تلقائيًا ويظهرها في لوحتك
4. ✅ انتهينا — قاعدة البيانات جاهزة

---

## الخطوة 5: انشر المنصة

1. في نفس اللوحة، اضغط **+ New** (أو **Add Service**)
2. اختر **GitHub Repository**
3. اختر مستودع `mir-platform`
4. Railway سيكتشف `railway.json` و `Dockerfile.railway` تلقائيًا ويبدأ البناء
5. انتظر 3–5 دقائق حتى يكتمل البناء (سترى شريط تقدم في تبويب **Deployments**)

✅ عند اكتمال البناء، المنصة تعمل لكن تحتاج رابطًا ومتغيرات.

---

## الخطوة 6: اربط قاعدة البيانات بالمنصة

1. اضغط على خدمة **PostgreSQL** في لوحة Railway
2. في تبويب **Connect**، انسخ **Database URL** (تبدأ بـ `postgresql://...`)
3. اضغط على خدمة **mir-platform** (التي نشرتها للتو)
4. اذهب إلى تبويب **Variables**
5. أضف المتغيرات التالية:

| المفتاح | القيمة |
|---|---|
| `DATABASE_URL` | (الصق الرابط الذي نسخته من PostgreSQL) |
| `MIR_SECRET` | (اضغط Generate لأنتاج مفتاح عشوائي، أو ضع أي نص طويل معقّد 32+ حرفًا) |

6. اضغط **Save** / **Deploy** لإعادة النشر بالمتغيرات الجديدة

---

## الخطوة 7: احصل على رابط الموقع

1. في خدمة `mir-platform`، اذهب إلى تبويب **Settings**
2. قسم **Networking** → اضغط **Generate Domain**
3. ستحصل على رابط مثل: `mir-platform-production.up.railway.app`
4. ✅ افتح الرابط — ستظهر شاشة تسجيل الدخول!

---

## الخطوة 8: سجّل الدخول لأول مرة

استخدم الحساب الإداري الذي أنشأه سكربت الإعداد تلقائيًا:

```
البريد: admin@mir.sa
كلمة المرور: Admin@Mir2026
```

### ⚠️ مهم جدًا — غيّر كلمة المرور فورًا:
1. بعد الدخول، اضغط على صورة المستخدم في أعلى يسار الصفحة → **الملف الشخصي**
2. في قسم **تغيير كلمة المرور**:
   - كلمة المرور الحالية: `Admin@Mir2026`
   - كلمة المرور الجديدة: (اختر كلمة مرور قوية)
3. اضغط **حفظ**

---

## الخطوة 9: أنشئ باقي المستخدمين

من قائمة **المستخدمون والصلاحيات** (في القائمة الجانبية)، أنشئ حسابات لـ:
- مدير التشغيل
- المدير المالي
- المحاسب
- الموظفين

كل مستخدم سيدخل ببريده وكلمة المرور التي تعينها له.

---

## 💰 التكلفة المتوقعة

| العنصر | شهري |
|---|---|
| Railway (خطة Hobby) | $5 |
| PostgreSQL (ضمن Railway) | ضمن الـ$5 |
| النطاق الفرعي `*.up.railway.app` | مجاني |
| شهادة SSL (HTTPS) | مجانية تلقائيًا |
| **الإجمالي** | **$5/شهر** |

> الرصيد الترحيبي (~$5) يكفي للشهر الأول مجانًا.

---

## 🌐 ربط نطاق مخصص (اختياري)

إذا أردت عنوانًا مثل `mir.sa` بدل `*.up.railway.app`:

1. اشترِ نطاقًا من أي مسجّل (مثل namecheap.com أو niecy.sa)
2. في Railway: Settings → Networking → **Custom Domain**
3. أدخل نطاقك `mir.yourdomain.com`
4. Railway سيعطيك سجل DNS — أضفه في لوحة تحكم مسجّل النطاق
5. انتظر 10 دقائق — سيعمل الموقع على نطاقك مع HTTPS تلقائيًا

---

## 🔄 النسخ الاحتياطي

Railway ينسخ قاعدة بيانات PostgreSQL تلقائيًا كل 24 ساعة ويحتفظ بـ 7 نسخ.

### لتنزيل نسخة يدويًا:
1. اذهب إلى خدمة PostgreSQL في Railway
2. تبويب **Data** → **Export** → ستحصل على ملف SQL
3. احفظه على جهازك

---

## 🛠️ الصيانة والتحديثات

### تحديث المنصة بكود جديد:
إذا أرسل لك مطوّر تحديثات:
1. ارفع الملفات الجديدة إلى GitHub (نفس خطوة 2-ج)
2. Railway سيكتشف التغيير تلقائيًا ويعيد النشر في 2–3 دقائق
3. بياناتك محفوظة (لا تُفقد عند إعادة النشر)

### مراقبة الأداء:
- تبويب **Metrics** في Railway يعرض استهلاك المعالج والذاكرة
- تبويب **Logs** يعرض سجلات التشغيل

---

## ❓ المشاكل الشائعة

### المشكلة: الموقع لا يفتح بعد النشر
**الحل**: اذهب إلى تبويب **Deployments** → اضغط على آخر نشر → شاهد **Logs**. إذا رأيت خطأ، انسخه وأرسله لي.

### المشكلة: خطأ "Database connection failed"
**الحل**: تأكد أن متغير `DATABASE_URL` صحيح (انسخه من جديد من خدمة PostgreSQL).

### المشكلة: نسيت كلمة مرور admin@mir.sa
**الحل**: في Railway، اذهب إلى خدمة mir-platform → تبويب **Settings** → **Command** → نفّذ:
```
bun scripts/reset-admin-password.ts
```
(سأنشئ لك هذا السكربت إن احتجته — أخبرني فقط)

---

## 📞 تحتاج مساعدة؟

إذا واجهت أي مشكلة في أي خطوة:
1. انسخ رسالة الخطأ كاملة
2. صف لي ما حدث بالضبط
3. سأساعدك في الحل فورًا

---

## ✅ قائمة التحقق النهائية

- [ ] حمّلت `mir-platform.tar.gz`
- [ ] أنشأت حساب GitHub ورفعت الكود
- [ ] أنشأت حساب Railway وربطته بـ GitHub
- [ ] أنشأت قاعدة بيانات PostgreSQL
- [ ] نشرت خدمة mir-platform
- [ ] أضفت متغيرات `DATABASE_URL` و `MIR_SECRET`
- [ ] حصلت على رابط الموقع
- [ ] سجّلت الدخول بـ admin@mir.sa
- [ ] غيّرت كلمة المرور
- [ ] أنشأت باقي المستخدمين

🎉 **مبروك! منصة مير تعمل الآن على الإنترنت.**
