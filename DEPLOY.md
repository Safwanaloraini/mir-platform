# دليل استضافة منصة «مير» — خطوة بخطوة

> هذا الدليل مكتوب لمن لا يملك خبرة برمجية. اتبع الخطوات بالترتيب.

---

## الفهم المبدئي

المنصة الآن تعمل في **بيئة تطوير تجريبية** — تُغلق بانتهاء الجلسة. لاستخدامها فعليًا في شركتك، تحتاج إلى:

| العنصر | الغرض | التكلفة التقريبية |
|---|---|---|
| **خادم (VPS)** | جهاز يعمل 24 ساعة يستضيف المنصة | 5–10 دولار/شهر |
| **اسم نطاق (Domain)** | عنوان الموقع مثل `mir.sa` | ~10–50 دولار/سنة |
| **شهادة أمان (SSL)** | قفل 🔒 بجانب العنوان | مجانية (تلقائية) |

**إجمالي تقريبي: 80–150 دولار/سنة** كاملًا.

---

## الخيار الموصى به: خادم Hetzner + Docker

لماذا Hetzner؟ رخيص (4 يورو/شهر)، سريع، موثوق، وموجود لديه مركز بيانات في أوروبا قريب من السعودية.

### الخطوة 1: استأجر خادمًا

1. اذهب إلى https://www.hetzner.com/cloud
2. أنشئ حسابًا وأضف رصيدًا (بطاقة ائتمان أو PayPal)
3. أنشئ خادمًا جديدًا (New Project → Add Server) بهذه المواصفات:
   - **الموقع**: Falkenstein (ألمانيا) أو Helsinki (فنلندا) — الأقرب للسعودية
   - **نظام التشغيل**: Ubuntu 24.04
   - **المواصفات**: CX22 (2 معالج، 4 جيجا رام) — يكفي لـ 20 مستخدمًا متزامنًا
   - **السعر**: ~4 يورو/شهر
4. ستصلك بيانات الدخول (IP + كلمة مرور root) على بريدك

### الخطوة 2: ارفع كود المنصة إلى الخادم

بعد استلام بيانات الخادم، استخدم أحد الخيارات:

**الخيار أ (الأبسط): ارفع الملفات مباشرة**
- استخدم برنامج FileZilla (مجاني) للرفع عبر SFTP
- اتصل بالخادم باستخدام IP وكلمة المرور
- ارفع مجلد المشروع كاملًا إلى `/root/mir`

**الخيار ب (للمطورين): عبر Git**
- ارفع الكود إلى GitHub (مستودع خاص)
- على الخادم: `git clone https://github.com/you/mir.git`

### الخطوة 3: ثبّت Docker على الخادم

افتح Terminal (أو استخدم SSH عبر Terminal في جهازك)، ثم نفّذ:

```bash
# اتصل بالخادم
ssh root@IP_الخادم

# انتقل لمجلد المشروع
cd /root/mir

# ثبّت Docker بأمر واحد
curl -fsSL https://get.docker.com | sh
```

### الخطوة 4: اضبط الإعدادات

```bash
# انسخ ملف الإعدادات النموذجي
cp .env.production.example .env.production

# عدّل القيم
nano .env.production
```

بدّل:
- `PUBLIC_URL=https://mir.yourdomain.com` (بدّل بنطاقك)
- `MIR_SECRET=` ضع مفتاحًا عشوائيًا طويلًا (ولّده عبر: `openssl rand -hex 32`)

احفظ: `Ctrl+X` ثم `Y` ثم `Enter`.

### الخطوة 5: شغّل المنصة

```bash
docker compose up -d --build
```

انتظر 3–5 دقائق حتى يكتمل البناء. ثم تحقق:

```bash
docker compose ps
```

سترى `mir-app` و `mir-backup` بحالة `running`.

المنصة الآن تعمل على `http://IP_الخادم:3000`

### الخطوة 6: اربط النطاق + فعّل HTTPS (مجاني)

```bash
# ثبّت Caddy (يعمل كبوابة وتلقائيًا يفعّل HTTPS)
apt install caddy -y

# اضبط الإعدادات
cat > /etc/caddy/Caddyfile << 'EOF'
mir.yourdomain.com {
    reverse_proxy localhost:3000
}
EOF

# أعد تشغيل Caddy
systemctl restart caddy
```

وجّه نطاقك (في لوحة تحكم مسجّل النطاق) إلى IP الخادم:
- سجل من نوع `A` باسم `mir` وقيمة `IP_الخادم`

انتظر 5–10 دقائق لانتشار DNS، ثم افتح `https://mir.yourdomain.com` — ستجد القفل 🔒 أخضر بجانب العنوان.

### الخطوة 7: أنشئ بياناتك

```bash
# أدخل حاوية التطبيق
docker compose exec mir sh

# شغّل سكربت البذور لإنشاء المستخدمين والبيانات الأولية
bun scripts/seed.ts

# غيّر كلمات المرور فورًا من داخل المنصة بعد أول دخول
exit
```

---

## بديل أبسط (أغلى قليلًا): Railway أو Render

إذا وجدت الخطوات أعلاه معقدة، يمكنك استخدام منصة تدير كل شيء تلقائيًا:

### Railway.app
1. ارفع الكود إلى GitHub
2. اذهب إلى https://railway.app
3. "New Project" → "Deploy from GitHub repo"
4. أضف Volume للحفاظ على قاعدة البيانات
5. اضبط `DATABASE_URL` و `MIR_SECRET`
- **التكلفة**: ~$5/شهر

**ملاحظة**: في Railway، غيّر `DATABASE_URL` إلى متغير Volume المسار.

---

## بديل محلي: خادم داخل مكتبك

إذا كان لديك جهاز كمبيوتر يعمل دائمًا في المكتب، يمكنك استضافته محليًا:

```bash
# على جهاز المكتب
cd mir
docker compose up -d --build
```

ثم اعرض المنصة داخل الشبكة المحلية على `http://IP_المكتب:3000`.

للوصول من خارج المكتب مجانًا، استخدم **Cloudflare Tunnel**:
```bash
# ثبّت cloudflared
curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o /usr/local/bin/cloudflared
chmod +x /usr/local/bin/cloudflared

# سجّل الدخول وأنشئ النفق
cloudflared tunnel login
cloudflared tunnel create mir
cloudflared tunnel route dns mir mir.yourdomain.com
cloudflared tunnel run --url http://localhost:3000 mir
```

---

## مهام ما بعد النشر (مهمة جدًا)

### 1. غيّر كلمات المرور الافتراضية
بعد أول دخول كـ `ceo@mir.sa`، اذهب إلى **الملف الشخصي** وغيّر كلمة المرور فورًا.

### 2. فعّل النسخ الاحتياطي التلقائي
حاوية `mir-backup` في `docker-compose.yml` تنشئ نسخة يومية في مجلد `backups/`. للتحقق:
```bash
ls -la /root/mir/backups/
```

لتنزيل نسخة احتياطية إلى جهازك:
```bash
scp root@IP_الخادم:/root/mir/backups/custom-*.db .
```

### 3. راقب الأداء
```bash
# حالة الخدمات
docker compose ps

# السجلات
docker compose logs -f mir

# استهلاك الموارد
docker stats
```

---

## الصيانة الدورية

### تحديث المنصة برمز جديد
```bash
cd /root/mir
git pull                          # أو ارفع الملفات الجديدة
docker compose up -d --build      # أعِد البناء والإقلاع (البيانات محفوظة)
```

### ترقية حجم الخادم
إذا زاد عدد المستخدمين عن 20، ترقَّ إلى CX32 (~7 يورو/شهر) من لوحة Hetzner.

---

## جدول التكلفة الكامل

| العنصر | شهري | سنوي |
|---|---|---|
| خادم Hetzner CX22 | 4 يورو | ~48 يورو |
| نطاق `.sa` | — | ~200 ريال |
| نطاق `.com` | — | ~$12 |
| شهادة SSL | مجانية | مجانية |
| النسخ الاحتياطي | مجاني (محلي) | مجاني |
| **الإجمالي التقريبي** | **~30 ريال/شهر** | **~400 ريال/سنة** |

---

## المساعدة

إذا أردت، أستطيع:
1. **تجهيز سكربت نشر آلي** يفعل كل الخطوات بأمر واحد
2. **إعداد Cloudflare Tunnel** للوصول بدون IP عام
3. **ترحيل قاعدة البيانات من SQLite إلى PostgreSQL** لو توقعت نموًا كبيرًا
4. **إعداد بريد إشعارات** (Gmail/SMTP) لتصل الإشعارات على إيميلك

أخبرني أي مسار تفضّل وسأجهّز لك ما يلزم خطوة بخطوة.
